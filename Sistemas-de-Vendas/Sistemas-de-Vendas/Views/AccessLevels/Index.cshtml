@model IEnumerable<Sistemas_de_Vendas.Models.AccessLevel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Level)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AccessLevelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AccessLevelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AccessLevelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
